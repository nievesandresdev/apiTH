{
    "name": "translation",
    "description": "Translate this hotel description into multiple target languages.",
    "parameters": {
        "type": "object",
        "properties": "{{ function_expected_parameters }}",
        "required": [
            "{{ expected_languages_keys }}"
        ]
    }
}